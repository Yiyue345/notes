---
title: "数据库系统原理课程报告填写模板"
description: "AI 生成的数据库系统原理复习资料。"
---

本文件按照考核要求中的课程报告结构整理为 Markdown 骨架。考试当天获得指定业务背景后，可以以此为组织思路填写内容。请注意：**最终报告必须依据当天公布的业务独立设计，并在所选数据库中实际运行SQL、补充真实截图**。

---

# 数据库系统原理课程报告

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 业务场景 | `填写当天公布的场景名称` |
| 使用数据库 | `MySQL / openGauss，二选一并说明版本或实验环境` |
| 姓名 |  |
| 学号 |  |
| 班级 |  |

---

# 模块一：数据库需求分析与概念、逻辑设计（30分）

## 1. 需求分析文档（10分）

> 要求：说明核心业务、实体和属性，总内容不超过2页。

### 1.1 系统背景与设计目标

```text
本系统针对【填写业务场景】进行数据库设计，主要服务对象包括【填写用户或工作人员角色】。
系统需要保存【填写核心数据对象】，支持【填写关键业务操作】等功能。
数据库设计目标是保证业务数据完整一致，并为录入、查询、统计和权限控制提供支持。
```

### 1.2 核心业务流程

1. `填写业务流程1，例如：管理员维护基础信息。`
2. `填写业务流程2，例如：用户发起业务申请。`
3. `填写业务流程3，例如：系统记录明细与处理状态。`
4. `填写业务流程4，例如：工作人员审核或完成处理。`
5. `填写业务流程5，例如：按条件进行统计查询。`

### 1.3 实体与属性分析

> 核心实体不少于5个，名称必须来自当天业务。

| 序号 | 实体名称 | 主键 | 主要属性 | 业务含义 |
| ---: | --- | --- | --- | --- |
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |
| 4 |  |  |  |  |
| 5 |  |  |  |  |
| 6（可选） |  |  |  |  |

### 1.4 主要业务规则与数据约束

| 规则编号 | 业务规则 | 转换为数据库约束的思路 |
| --- | --- | --- |
| R1 |  | `PRIMARY KEY / UNIQUE / NOT NULL / CHECK / FOREIGN KEY` |
| R2 |  |  |
| R3 |  |  |
| R4 |  |  |

### 1.5 主要查询需求

| 查询编号 | 查询需求描述 | 预计涉及实体 |
| --- | --- | --- |
| Q1 |  |  |
| Q2 |  |  |
| Q3 |  |  |
| Q4 |  |  |
| Q5 |  |  |

---

## 2. E-R图（10分）

> 在最终报告中插入绘制完成的E-R图截图或导出图片。

### 2.1 图中必须显示的内容

- [ ] 核心实体不少于5个。
- [ ] 每个实体标明主键及主要属性。
- [ ] 实体之间的联系名称清楚。
- [ ] 所有关键联系标明 `1:1`、`1:N` 或 `M:N`。
- [ ] 对多对多联系考虑其自身属性，例如数量、日期或状态。

### 2.2 E-R图位置

```text
【在此插入E-R图，并添加图题：图1  ______系统E-R图】
```

### 2.3 联系解释

| 联系两端实体 | 联系类型 | 联系含义 | 逻辑表转换方式 |
| --- | --- | --- | --- |
|  | `1:1 / 1:N / M:N` |  |  |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |

---

## 3. E-R模型向关系模式转换（10分）

### 3.1 关系模式汇总

> 使用下划线、加粗或文字标注主键，明确外键和约束。

| 表名 | 字段组成 | 主键 | 外键 | 非空、唯一、检查等约束 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

### 3.2 转换说明

```text
实体【】转换为关系【】，其主键为【】。
实体【】与实体【】之间为一对多联系，因此在多端关系【】中加入外键【】。
实体【】与实体【】之间为多对多联系，因此建立联系关系【】，以【】作为联合主键，并保存联系自身属性【】。
```

### 3.3 完整性设计说明

| 完整性类型 | 本系统实现方式 | 示例约束或字段 |
| --- | --- | --- |
| 实体完整性 |  |  |
| 参照完整性 |  |  |
| 用户自定义完整性 |  |  |

---

# 模块二：SQL查询编程实现（20分）

## 1. 数据库环境说明

```text
本实验使用【MySQL / openGauss】数据库完成SQL代码编写和运行验证。
数据库名称为【】，主要数据表包括【】。
```

---

## 2. DDL语句：数据库与表创建（6分）

> 要求：不少于3张表，包含实体完整性、参照完整性和自定义完整性。

### 2.1 创建数据库代码

```sql
-- 在此填写并运行创建数据库的SQL
```

### 2.2 创建数据表代码

```sql
-- 在此填写所有数据表的CREATE TABLE语句
-- 应包含PRIMARY KEY、FOREIGN KEY、NOT NULL、UNIQUE或CHECK等约束
```

### 2.3 运行结果截图

```text
【插入数据库创建或表结构验证截图】
```

### 2.4 约束说明

| 约束代码位置 | 约束类型 | 业务作用 |
| --- | --- | --- |
|  | 实体完整性 |  |
|  | 参照完整性 |  |
|  | 用户自定义完整性 |  |

---

## 3. DML语句：数据插入、更新与删除（6分）

### 3.1 批量插入3条数据

所选数据表：`填写表名`

AI使用与本人修改说明：

```text
本部分基础样例数据由AI辅助生成。本人与业务字段和约束逐项核对后，对【填写修改内容】进行了调整，并在数据库中执行验证。
```

```sql
-- INSERT INTO ... VALUES
```

运行结果：

```text
【插入执行后SELECT查询结果截图】
```

### 3.2 数据更新

中文需求：

```text
填写自己设计的更新要求。
```

```sql
-- UPDATE ...
```

运行结果：

```text
【更新前后结果截图】
```

### 3.3 数据删除

中文需求：

```text
填写自己设计的删除要求，并说明是否受外键约束影响。
```

```sql
-- DELETE ...
```

运行结果：

```text
【删除执行及结果截图】
```

---

## 4. 多表查询语句（5分）

> 要求：自定义5组涉及2张及以上表的查询，并覆盖 `LIKE`、`IN`、`JOIN`、`GROUP BY` 等。

### 查询1：含 `LIKE` 的多表查询

需求描述：

```text
```

```sql
-- SQL代码
```

结果截图：`【插图】`

### 查询2：含 `IN` 或子查询的多表查询

需求描述：

```text
```

```sql
-- SQL代码
```

结果截图：`【插图】`

### 查询3：含 `JOIN` 的业务明细查询

需求描述：

```text
```

```sql
-- SQL代码
```

结果截图：`【插图】`

### 查询4：含 `GROUP BY` 的统计查询

需求描述：

```text
```

```sql
-- SQL代码
```

结果截图：`【插图】`

### 查询5：自定义综合查询

需求描述：

```text
```

```sql
-- SQL代码
```

结果截图：`【插图】`

---

## 5. 视图设计（3分）

### 5.1 视图需求说明

```text
本系统中【填写复杂查询用途】属于高频查询，因此建立视图【填写视图名】，用于简化【填写用途】。
```

### 5.2 创建视图代码

```sql
-- CREATE VIEW ...
```

### 5.3 查询视图代码与结果

```sql
-- SELECT ... FROM 视图名;
```

```text
【插入视图查询结果截图】
```

---

# 模块三：数据库事务与故障恢复设计（10分）

## 1. 关键事务说明

```text
本系统中的关键事务为【填写业务操作】，其涉及【填写多项关联更新】。这些操作应作为一个整体执行，以保证事务的原子性和数据库一致性。
```

## 2. 故障类型分析（5分）

| 故障类型 | 本系统可能发生的场景 | 产生影响 | 恢复策略 |
| --- | --- | --- | --- |
| 事务故障 |  |  | `UNDO / 回滚事务` |
| 系统故障 |  |  | `UNDO未提交，REDO已提交` |
| 介质故障 |  |  | `恢复备份并应用日志` |

## 3. 备份与恢复方案（5分）

### 3.1 日常备份策略

| 备份类型 | 执行频率 | 保存位置 | 目的 |
| --- | --- | --- | --- |
| 全量备份 |  |  |  |
| 增量备份 |  |  |  |
| 日志归档 |  |  |  |

### 3.2 日志应用方案

```text
填写日志保存的内容、检查点策略，以及事务故障、系统故障、介质故障中如何使用日志完成UNDO或REDO。
```

### 3.3 故障恢复流程

```text
1. 
2. 
3. 
4. 
```

---

# 模块四：数据库安全设计（10分）

## 1. 用户与角色权限体系（4分）

| 用户或角色 | 业务职责 | 允许访问的对象 | 授予的权限 | 设计理由 |
| --- | --- | --- | --- | --- |
| 管理员 |  |  |  |  |
| 操作员 |  |  |  |  |
| 查询统计人员 |  |  |  |  |

设计原则说明：

```text
填写如何体现最小权限原则，以及哪些敏感操作不向普通角色开放。
```

## 2. DCL代码实现（3分）

> 使用与模块二一致的数据库类型，代码应实际运行。

```sql
-- 创建用户

-- 创建角色

-- 授予权限

-- 将角色分配给用户

-- 回收某项权限
```

运行结果截图：

```text
【插入执行DCL语句及验证权限结果的截图】
```

## 3. 数据库安全防护策略（3分）

| 安全方向 | 本系统采取的方案 |
| --- | --- |
| 数据访问安全 |  |
| 密码与账号安全 |  |
| 非法访问和SQL注入防范 |  |
| 防止数据泄漏 |  |
| 审计与备份保护 |  |

---

# 模块五：总结反思（可选加分）

## 1. 设计与实现总结

```text
填写自己从需求分析、E-R建模到SQL实现的主要工作，以及设计中最重要的取舍。
```

## 2. SQL调试与问题解决

| 遇到的问题或错误 | 可能原因 | 修改方法 | 最终验证结果 |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

## 3. AI辅助使用与本人改进说明

```text
填写哪些基础内容参考或借助了AI，自己如何修改表结构、数据、SQL或文字表述，以及如何通过实际运行验证正确性。
```

---

# 最终提交核对表

- [ ] 所有内容使用当天公布业务背景，不提交固定示例答案。
- [ ] 需求分析在2页以内。
- [ ] E-R图实体数量、属性、主键与联系基数齐全。
- [ ] 关系模式和SQL表结构保持一致。
- [ ] 所有SQL在所选数据库中运行验证并附截图。
- [ ] 五组查询覆盖指定关键词，并且均为多表查询。
- [ ] 恢复与安全方案明确结合本系统业务。
- [ ] 报告按格式要求调整为小四字号、20磅行间距。
- [ ] AI辅助内容已说明本人设计、修改和验证过程。
- [ ] 在规定截止时间前提交到头歌对应作业位置。
